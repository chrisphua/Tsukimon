import 'package:flutter/material.dart';

class CancelledPageWidget extends StatelessWidget {
  const CancelledPageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
