import 'package:flutter/material.dart';

class FinishedPageWidget extends StatelessWidget {
  const FinishedPageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}